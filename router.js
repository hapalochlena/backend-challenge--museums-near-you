const express = require('express');
const router = express.Router();

// const {} = require('./controller');

router.post('/', ); // receive the lat + long
router.get('/museums', ); // display the museums

// router.get('/api/query', );

module.exports = router;
